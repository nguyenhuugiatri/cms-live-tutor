import { Menu } from 'antd';
import styled from 'styled-components';
import { COLOR } from '../../../../styles/colorPalette';
export const StyledMenu = styled(Menu)`
  background-color: ${COLOR.SEMANTIC.GREY[2]} !important;
`;
